# Lightweight requirements for CI — no heavy AWS SDK optional deps
# Strands is NOT installed in CI since it requires AWS credentials to import
# The code gracefully falls back when strands is not available (try/except at top of each file)

boto3>=1.34.0
botocore>=1.34.0
streamlit>=1.35.0
pandas>=2.0.0
python-dotenv>=1.0.0
python-dateutil>=2.9.0
pyarrow>=14.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
